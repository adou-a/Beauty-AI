[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "beauty-ai"
version = "0.1.0"
description = "AI skincare assistant"
requires-python = ">=3.10"
dependencies = []


[tool.setuptools.packages.find]
where = ["."]